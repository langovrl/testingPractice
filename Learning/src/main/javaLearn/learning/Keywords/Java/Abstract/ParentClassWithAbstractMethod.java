package learning.Keywords.Java.Abstract;

public class ParentClassWithAbstractMethod {
	
	
	// If any one method is abstract then class should be declared as a abstract class
	// Error : The abstract method wheel in type ParentClassWithAbstractMethod can only be defined by an abstract class
	
	
	//public abstract void wheel();

	/*
	Abstract methods:-
	The method which is having declaration but not implementations such type of methods are called abstract Method. Hence every abstract method should end with “;”.
	The child classes are responsible to provide implementation for parent class abstract methods.*/
}
